<?php declare(strict_types = 1);

/*
Use the Magic Constant to return the current working directory then concatenate the string to move a directory up then into the src directory for bootstrapping.
*/
require __DIR__ . '/../src/Bootstrap.php';
